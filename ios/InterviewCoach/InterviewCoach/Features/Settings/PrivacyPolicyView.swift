import SwiftUI

struct PrivacyPolicyView: View {
    var body: some View {
        List {
            Section("数据存储") {
                Text("简历原文仅保存在你的设备本地（SwiftData），不会上传到服务器持久化存储。")
                Text("AI Provider 的 API Key 在服务端使用 AES-GCM 加密保存，不会以明文返回给客户端。")
            }

            Section("简历隐私") {
                Text("当你使用「生成摘要」功能时，简历原文会临时发送到后端进行 AI 摘要生成。")
                Text("后端仅在内存中使用原文，不会写入数据库，也不会记录到日志。")
                Text("你确认摘要后，服务端只保存结构化摘要内容，不保留原文。")
            }

            Section("教练记忆") {
                Text("训练、测评和模拟面试过程中，AI 会沉淀结构化教练记忆（能力短板、强项、进步趋势）。")
                Text("教练记忆保存在服务端，用于后续训练和面试的个性化指导。")
                Text("本机也会保留一份教练记忆归档（CoachingMemoryArchive），方便离线复盘。")
            }

            Section("账号删除") {
                Text("删除账号将永久清除服务端所有数据，包括目标岗位、候选人画像、测评记录、训练计划、模拟面试、报告和远端教练记忆。")
                Text("本机教练记忆归档默认保留在当前设备上，方便你继续复盘或后续手动导入。")
                Text("如果你也希望删除本机记忆，可以在删除账号时勾选「同时删除本机教练记忆文件」。")
                Text("Keychain 中的登录凭证会在删除账号或退出登录时清除。")
            }

            Section("重新登录与记忆导入") {
                Text("重新登录或注册新账号时，如果检测到本机历史教练记忆归档，App 会提示你审查并选择是否导入。")
                Text("导入需要你主动确认，不会自动上传。你可以选择全部导入、部分导入或拒绝导入。")
                Text("拒绝导入的记忆不会参与后续 AI 训练和面试。")
            }

            Section("数据隔离") {
                Text("不同用户之间无法互相访问彼此的数据。所有业务查询均包含用户身份校验。")
            }
        }
        .navigationTitle("隐私政策")
        .navigationBarTitleDisplayMode(.inline)
    }
}
