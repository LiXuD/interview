services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: interview_coach
      POSTGRES_USER: ic_user
      POSTGRES_PASSWORD: ic_dev_password
    ports:
      - "5432:5432"
    volumes:
      - pg_data:/var/lib/postgresql/data

volumes:
  pg_data:
